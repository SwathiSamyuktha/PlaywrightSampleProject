import { BasePage } from './BasePage';

/** Signup page object — extend with selectors and actions for your app. */
export class SignupPage extends BasePage {
  // Add getters and methods for signup form when needed
}
