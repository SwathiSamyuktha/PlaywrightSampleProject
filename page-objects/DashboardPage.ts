import { BasePage } from './BasePage';

/** Dashboard page object — extend with selectors and actions for your app. */
export class DashboardPage extends BasePage {
  // Add getters and methods for dashboard when needed
}
