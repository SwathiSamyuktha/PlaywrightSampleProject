import { BasePage } from './BasePage';

/** User profile page object — extend with selectors and actions for your app. */
export class ProfilePage extends BasePage {
  // Add getters and methods for profile when needed
}
